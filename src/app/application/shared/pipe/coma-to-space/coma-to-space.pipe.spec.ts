import { ComaToSpacePipe } from './coma-to-space.pipe';

describe('ComaToSpacePipe', () => {
  it('create an instance', () => {
    const pipe = new ComaToSpacePipe();
    expect(pipe).toBeTruthy();
  });
});
